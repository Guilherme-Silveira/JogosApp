<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.semantix.jogosapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Cadastro" />
        <activity
            android:name=".MeusJogos"
            android:label="@string/title_activity_meus_jogos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TodosUsu" />
        <activity android:name=".AlterarUsu" />
        <activity android:name=".CadastroCategoria" />
        <activity android:name=".TodosCat" />
        <activity android:name=".AlterarCat" />
        <activity android:name=".CadastroJogo" />
        <activity android:name=".TodosJogos" />
        <activity android:name=".AlterarJogo"></activity>
    </application>

</manifest>